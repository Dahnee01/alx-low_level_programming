#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x: an integer to add to y
 * @y: an integer to add to b
 * Return: Void.
 */
int add(int x, int y)
{
int n;
n = x + y;
return (n);
}
