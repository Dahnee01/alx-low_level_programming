#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: an integer to add to b
 * @b: an integer to add to a
 * Return: Void.
 */
int add(int x, int y)
{
int n;
n = x + y;
return (n);
}
