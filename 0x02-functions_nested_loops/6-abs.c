#include "main.c"
/**
 * _abs - returns absolute value of an int
 * @r: integer from which to get an absolute value
 * Return: Void.
 */
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (r * -1);
}
}
